@model IEnumerable<Teste_TGS.ViewModels.ClienteViewModel>

@{
    ViewData["Title"] = "Gerenciamento de Clientes";
}

<h1>CLIENTES</h1>

<table class="table">
    <thead class="container">
        <tr class="row">
            <th class="col-sm-3">
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th class="col-sm-2">
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th class="col-sm-4">
                @Html.DisplayNameFor(model => model.Logotipo)
            </th>
            <th class="col-sm"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="row">
                <td class="col-sm-3">
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td class="col-sm-2">
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td class="col-sm-4">
                    @Html.DisplayFor(modelItem => item.Logotipo)
                </td>
                <td class="col-sm"> *
                    @* @if (User.IsInRole("Master"))
                { *@
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Editar</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Visualizar</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Excluir</a>
                    @* }
                else if (User.IsInRole("Avançado"))
                {
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Editar</a>
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Visualizar</a>
                }
                else
                {
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Visualizar</a>
                } *@
                </td>
            </tr>
        }
    </tbody>
</table>